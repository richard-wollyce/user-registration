name: CD - Package & Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Java 25
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven

      # Step 3: Build JAR package
      - name: Build package
        run: mvn clean package -DskipTests -B

      # Step 4: Upload artifact for future use or deploy
      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: cadastro-usuario-jar
          path: target/*.jar

      # Step 5: (Optional) Deploy step placeholder
      # Example: Deploy to a remote server or hosting platform
      # - name: Deploy to production server
      #   run: |
      #     scp target/*.jar user@server:/apps/cadastro-usuario/
